# bash_scripts

This repository contains various bash scripts.

## list of bash scripts and their uses

**loglines.sh** - This script adds a correlative number at the beginning of each line separated by a set of dashes from the actual message
